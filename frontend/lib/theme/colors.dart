import 'package:flutter/material.dart';

class AppColors {
  // primary colors
  static const primaryDarkTeal = Color(0xFF2F3E46);
  static const primaryLightTeal = Color(0xFF354F52);
  static const mutedGreen = Color(0xFF52796F);

  // surface colors (for panels, cards, etc.)
  static const surfaceMint = Color(0xFF84A98C);
  static const surfaceLightGray = Color(0xFFCAD2C5);
  static const surfaceOffWhite = Color(0xFFFEFCF0);
  static const white = Color(0xFFFFFFFF);
  static const backgroundGray = Color(0xFFEAEAEA);
}